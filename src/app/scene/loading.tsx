import { Html } from '@react-three/drei';
import { Loader } from '../../styles/scene';

const Loading = () => {
  return (
    <Html center>
      <Loader />
    </Html>
  );
}

export default Loading;
